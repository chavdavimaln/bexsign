# 19 — Security Architecture

- **Auth Tokens**: Cryptographically strong random signing tokens (`/sign/:token`).
- **Data Hashing**: SHA-256 document fingerprinting for audit trail verification.
- **Access Control**: Role-Based Access Control (RBAC) & IP audit logging.
